# CLAUDE.md

**Read [AGENTS.md](AGENTS.md) first — it is the map for this worktree.**

This worktree (`bm/feat/evolution-sft-rl`) has a distinct job from `main`: it
executes the **Evolution → SFT → RL** training program, not the Deep Researcher
harness itself. `AGENTS.md` carries the job, the Linear milestone status, the baked-in
decisions, the stack, and how to work here. Don't reconstruct any of that from
memory — follow `AGENTS.md` and the Linear milestone (`Evolution → SFT → RL`) it
points to.
